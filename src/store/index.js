import { createStore } from 'vuex'

export default createStore({
  state: {
    // a: 0,
    // b: 1,
    // checked: false
  },
  mutations: {
    // checkedInputs(state) {
    //   if(state.b > 1) {
    //
    //   }
    // }
  },
  actions: {
  },
  modules: {
  }
})
